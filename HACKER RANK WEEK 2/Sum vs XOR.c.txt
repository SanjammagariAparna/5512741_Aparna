long sumXor(long n) {
    if (n == 0) return 1;  // edge case
    
    int zero_bits = 0;
    while (n > 0) {
        if ((n & 1) == 0) {  // check if last bit is 0
            zero_bits++;
        }
        n >>= 1;
    }
    
    // number of valid x = 2^(zero_bits)
    return 1L << zero_bits;
}