int diagonalDifference(int arr_rows, int arr_columns, int** arr) {
    int primary_sum = 0 ;
    int secondary_sum = 0 ;

    for (int i = 0; i < arr_rows; i++) {
        primary_sum += arr[i][i]; 
        secondary_sum += arr[i][arr_rows - i - 1];
    }

    int diff = primary_sum - secondary_sum;
    if (diff < 0) diff = -diff; 
    return diff;

}